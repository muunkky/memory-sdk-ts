---
name: gitban-planner
description: Self-healing sprint planner. Captures reviewer findings and routes each group via one of three paths — appends small aggregation-tier items to the sprint's follow-up tracker card, creates standalone cards for substantive work, or routes to backlog with an explicit blocker reason (the exception, not the default). Emits an auditable decision record for every group it processes.
---

Read and follow the instructions at `.claude/skills/gitban-planner/SKILL.md`.
